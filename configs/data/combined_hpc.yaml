data:
  class_path: mst.dataloaders.combined.CombinedDataModule
  init_args:
    medley_rootdir: 
    - /data/scratch/acw639/Medley/V1
    - /data/scratch/acw639/Medley/MedleyDB_V2/V2
    cambridge_rootdir:
    - /data/scratch/acw639/Cambridge/mixing-secrets
    length: 820000
    min_tracks: 1
    max_tracks: 16
    batch_size: 2
    num_workers: 4
    train_buffer_size_gb: 2.0
    val_buffer_size_gb: 0.1

#for now you have to go to combined.py in mst/dataloaders and change the root directory for both the datasets